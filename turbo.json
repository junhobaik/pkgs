{
  "$schema": "https://turbo.build/schema.json",
  "pipeline": {
    "ui#build": {
      "dependsOn": ["^build"],
      "outputs": ["dist/**"]
    },

    "ui#watch": {
      "dependsOn": ["^build"],
      "outputs": ["dist/**"],
      "cache": false,
      "persistent": true
    },

    "ui#storybook": {
      "cache": false,
      "persistent": true
    },

    "ui#storybook-build": {
      "dependsOn": ["^build"],
      "outputs": ["dist_sb/**"]
    },

    "dev": {
      "cache": false,
      "persistent": true
    }
  }
}
